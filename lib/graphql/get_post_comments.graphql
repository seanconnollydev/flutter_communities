# import './post_comment_fragment.graphql'

query GetPostComments($postId: ID!, $cursor: String) {
  findPostByID(id: $postId) {
    _id
    comments(_size: 10, _cursor: $cursor) {
      data {
        ...PostCommentFragment
      }
    }
  }
}